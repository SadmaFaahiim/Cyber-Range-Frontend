// Conditional setup file (Phase 1 skeleton had no unit-test setup; generated in Phase 6).
// Remove this file and the `setupFiles` entry in vite.config.ts only if it is ever
// replaced by an equivalent global test setup.
import '@testing-library/jest-dom';
